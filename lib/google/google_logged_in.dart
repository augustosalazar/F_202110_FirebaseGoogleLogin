import 'package:flutter/material.dart';

class GoogleLoggedIn extends StatelessWidget {
  const GoogleLoggedIn({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Logged in")),
    );
  }
}
